※ Java注解
    1.注解是什么？
        注解是Java 1.5以来引入的特性，是一种用来解释代码，可以标注在字段，方法，类，接口，包，方法参数，局部变量前表示某种意思，它也叫元数据。
        一般注解有三种：
        a.元注解
          说明：元注解，是用于定义注解的注解。
          包括@Retention(标明被保留的阶段),@Target(使用范围),@Inherited(标明可继承),@Document(生成javadoc文档),@Repeatable(1.8新加入，可重复的)
        b.Java自带的标准注解
          说明：Java中自带了些用于标注代码的注解，如：@Override,@Deprecated等等。
        c.自定义注解
          说明：自定义合适场景的注解，注解的扩展。
    2.注解的用途
        a.生成文档
        b.跟踪代码依赖，实现配置文件功能
        c.在编译时检测格式
        d.在运行时动态处理
    3.注解的使用
        a.标注在class interface enum的注解
        b.标注在method
        c.标注在field
        d.标注在parameter
    4.注解的实现原理
